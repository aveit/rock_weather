# Automatically generated on 2021-07-27 UTC from https://codemagic.io/app/61007bbcd885ed180223ee74/settings
# Note that this configuration is not an exact match to UI settings. Review and adjust as necessary.

workflows:
  default-workflow:
    name: Default Workflow
    max_build_duration: 60
    environment:
      flutter: 1.22.6
      xcode: latest
      cocoapods: default
    triggering:
      events:
        - push
      branch_patterns:
        - pattern: "*"
          include: true
          source: true
      tag_patterns:
        - pattern: "*"
          include: true
    scripts:
      - flutter packages pub get
      - flutter analyze
      - cd . && flutter test test
    artifacts:
      - flutter_drive.log
    publishing:
      email:
        recipients:
          - acacio.veit.schneider@gmail.com
